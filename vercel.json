{
  "version": 2,
  "routes": [
    { "src": "/api/(.*)", "dest": "/server" },
    { "src": "/health", "dest": "/server" },
    { "src": "/proxy/(.*)", "dest": "/server" },
    { "src": "/(.*)", "dest": "/public/$1" }
  ]
}
